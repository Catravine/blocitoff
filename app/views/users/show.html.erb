<% @username = @user.email.slice(0...(@user.email.index('@'))).capitalize %>
<h1>Profile: <span class="highlight"><%= @username %></h1>
<div class="row">
  <div class="col-md-8">
    <p>Here is a user profile for <%= @user.email %>. Have tons of fun with it.</p>
    <ul>
      <li><strong>Email:</strong> <%= @user.email %></li>
      <li><strong>Blocing it off since:</strong> <%= time_ago_in_words(@user.created_at) %> ago.</li>
      <li><strong>Password:</strong> ...yes.
    </ul>
    <p><%= link_to "Edit profile", edit_user_registration_path, class: 'btn btn-primary' %></p>
  </div>
  <div class="col-md-4">
    <div class="lists">
      <h3><%= @username %>'s To-Dos:</h3>
      <ul>
        <%= render partial: 'items/item', locals: { user: @user } %>
      </ul>
    </div>
    <%= render 'items/form', item: Item.new, user: @user %>
  </div>
</div>
